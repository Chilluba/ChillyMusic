# Dependencies
node_modules/
.pnp
.pnp.js

# Build artifacts
dist/
build/
out/
coverage/
*.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Editor directories and files
.idea
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace

# System files
.DS_Store
Thumbs.db

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Optional eslint cache
.eslintcache

# Prisma (if used, as per PRD)
prisma/generated/
prisma/dev.db # Example SQLite db
prisma/dev.db-journal

# Compiled JavaScript files from TypeScript (if tsc is run in src)
# It's better to output to a 'dist' or 'build' folder, but if not:
# src/**/*.js
# src/**/*.js.map
# src/**/*.d.ts (if declaration files are alongside .js files)
